import React, { useState } from 'react';
import Header from '../../components/Header';
import LoginContext from '../globals/globalContext';

const AchievementScreenComponent = () => {

    return (
        <div>
            <Header/>
        </div>
    );
};

export default AcievementsOverviewScreenComponent;